<!DOCTYPE html>
<html lang="en">
<head>
    <title>Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            display: flex;
            flex-direction: column;
        }

       
        .header {
            background: #007bff;
            color: white;
            padding: 12px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 18px;
            flex-wrap: wrap;
        }

        .header a {
            color: white;
            text-decoration: none;
            margin-left: 15px;
            padding: 8px 15px;
            border-radius: 5px;
            transition: 0.3s;
        }

        .header a:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        
        @media (max-width: 600px) {
            .header {
                flex-direction: column;
                text-align: center;
                padding: 15px;
            }

            .header a {
                margin: 5px 0;
                display: block;
            }
        }

       
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding: 20px;
            width: 100%;
        }

        .container {
            width: 90%;
            max-width: 800px;
            padding: 25px;
            background: white;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
        }

        
        .create-blog {
            text-align: center;
            margin-bottom: 20px;
        }

        .create-blog a {
            display: inline-block;
            padding: 10px 20px;
            background: #28a745;
            color: white;
            text-decoration: none;
            font-weight: bold;
            border-radius: 5px;
            transition: 0.3s;
        }

        .create-blog a:hover {
            background: #218838;
        }

       
        .blog-list {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .blog-item {
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background: #ffffff;
            transition: 0.3s;
        }

        .blog-item:hover {
            background: #f9f9f9;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }

        .blog-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
        }

        .blog-header h4 {
            margin: 0;
            font-size: 18px;
            color: #333;
        }

        .blog-date {
            font-size: 14px;
            color: #777;
        }

        .blog-content {
            font-size: 15px;
            color: #444;
            line-height: 1.5;
            margin-bottom: 10px;
        }

        .blog-actions {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
        }

        .blog-actions a {
            text-decoration: none;
            padding: 6px 12px;
            font-size: 14px;
            border-radius: 5px;
            transition: 0.3s;
        }

        .edit-btn {
            background: #007bff;
            color: white;
        }

        .edit-btn:hover {
            background: #0056b3;
        }

        .delete-btn {
            background: #dc3545;
            color: white;
        }

        .delete-btn:hover {
            background: #a71d2a;
        }

       
        .footer {
            background: #343a40;
            color: white;
            text-align: center;
            padding: 12px;
            font-size: 14px;
            width: 100%;
            margin-top: auto;
        }


        @media (max-width: 768px) {
            .container {
                width: 95%;
            }

            .blog-item {
                padding: 12px;
            }

            .blog-header {
                flex-direction: column;
                text-align: center;
            }

            .blog-actions {
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>My Blog</h2>
        <div>
            <a href="/posts">Dashboard</a>
            <a href="/logout">Logout</a>
        </div>
    </div>

    <div class="main-content">
        <div class="container">
            <h2>Welcome, <%= user.name %>!</h2>

            <div class="create-blog">
                <a href="/posts/new">+ Create New Blog</a>
            </div>

            <h3>Your Blogs</h3>

            <div class="blog-list">
                <% if (blogs.length === 0) { %>
                    <p>No blogs found. Start writing your first blog!</p>
                <% } else { %>
                    <% blogs.forEach(blog => { %>
                        <div class="blog-item">
                            <div class="blog-header">
                               
                                <p><strong>Title:</strong> <span class="blog-title"><%= blog.title %></span></p>
                                <span class="blog-date">🕒 <%= blog.createdAt %> (IST)</span>
                            </div>
                        
                            <p><strong>Content:</strong></p>
                            <p class="blog-content"><%= blog.content %></p>
                            <div class="blog-actions">
                                <a href="/posts/edit/<%= blog.id %>" class="edit-btn">✏️ Edit</a>
                                <a href="/posts/delete/<%= blog.id %>" class="delete-btn" onclick="return confirm('Are you sure you want to delete this post?');">🗑️ Delete</a>
                            </div>
                        </div>
                    <% }) %>
                <% } %>
            </div>
        </div>
    </div>

    <div class="footer">
        <p>&copy; 2025 My Blog. All rights reserved.</p>
    </div>
</body>
</html>
